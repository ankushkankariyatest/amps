(function() {
    "use strict";
    define(['assetIt', 'auth-constants', 'auth-model'], function(app) {
        app.register.controller('authMessageController', ['$scope',
            function(scope) {
                scope.initializeController = function() {
                    
                };
            }
        ]);
    });
})();